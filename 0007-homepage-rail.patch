--- a/lib/features/home/home_page.dart
+++ b/lib/features/home/home_page.dart
@@ -1,6 +1,5 @@
 
 import 'package:flutter/material.dart';
-import '../../core/models/connection_strategy.dart';
 import '../account/account_section.dart';
 import '../advanced/advanced_mode_page.dart';
 import '../simple/simple_mode_page.dart';
@@ -19,69 +18,546 @@
 
   @override
   Widget build(BuildContext context) {
+    final scheme = Theme.of(context).colorScheme;
+
     return Scaffold(
+      backgroundColor: scheme.surface,
       body: SafeArea(
-        child: Padding(
-          padding: const EdgeInsets.all(20),
-          child: Column(
-            crossAxisAlignment: CrossAxisAlignment.stretch,
-            children: [
-              _TopBar(
-                mode: _mode,
-                onModeChanged: (mode) => setState(() => _mode = mode),
-              ),
-              const SizedBox(height: 20),
-              Expanded(
-                child: AnimatedSwitcher(
-                  duration: const Duration(milliseconds: 220),
-                  child: _mode == AppMode.simple
-                      ? const SimpleModePage(key: ValueKey('simple'))
-                      : const AdvancedModePage(key: ValueKey('advanced')),
+        child: Column(
+          crossAxisAlignment: CrossAxisAlignment.stretch,
+          children: [
+            _WindowHeader(
+              mode: _mode,
+              onModeChanged: (mode) => setState(() => _mode = mode),
+            ),
+            _ModeBar(
+              mode: _mode,
+              onModeChanged: (mode) => setState(() => _mode = mode),
+            ),
+            Expanded(
+              child: Padding(
+                padding: const EdgeInsets.fromLTRB(18, 16, 18, 18),
+                child: Row(
+                  crossAxisAlignment: CrossAxisAlignment.stretch,
+                  children: [
+                    SizedBox(
+                      width: 300,
+                      child: _Sidebar(mode: _mode),
+                    ),
+                    const SizedBox(width: 16),
+                    Expanded(
+                      child: _Workspace(
+                        mode: _mode,
+                        child: AnimatedSwitcher(
+                          duration: const Duration(milliseconds: 220),
+                          child: _mode == AppMode.simple
+                              ? const SimpleModePage(key: ValueKey('simple'))
+                              : const AdvancedModePage(key: ValueKey('advanced')),
+                        ),
+                      ),
+                    ),
+                  ],
                 ),
               ),
-              const SizedBox(height: 16),
-              const AccountSection(),
-            ],
-          ),
+            ),
+          ],
         ),
       ),
     );
   }
 }
 
-class _TopBar extends StatelessWidget {
+class _WindowHeader extends StatelessWidget {
   final AppMode mode;
   final ValueChanged<AppMode> onModeChanged;
 
-  const _TopBar({
+  const _WindowHeader({
     required this.mode,
     required this.onModeChanged,
   });
 
   @override
   Widget build(BuildContext context) {
-    return Row(
+    final scheme = Theme.of(context).colorScheme;
+    final activeText = mode == AppMode.simple ? 'Quick connect ready' : 'Manual control ready';
+
+    return Container(
+      height: 38,
+      decoration: BoxDecoration(
+        color: scheme.surface,
+        border: Border(bottom: BorderSide(color: scheme.outline)),
+      ),
+      padding: const EdgeInsets.only(left: 12, right: 8),
+      child: Row(
+        children: [
+          Container(
+            width: 14,
+            height: 14,
+            decoration: BoxDecoration(
+              borderRadius: BorderRadius.circular(3),
+              gradient: LinearGradient(
+                colors: [scheme.primary, scheme.primary.withOpacity(0.55)],
+                begin: Alignment.topLeft,
+                end: Alignment.bottomRight,
+              ),
+            ),
+          ),
+          const SizedBox(width: 8),
+          const Text(
+            'OwOCloak',
+            style: TextStyle(fontSize: 12, fontWeight: FontWeight.w700),
+          ),
+          const SizedBox(width: 10),
+          Text(
+            activeText,
+            style: TextStyle(
+              fontFamily: 'Consolas',
+              fontSize: 10.5,
+              color: scheme.onSurfaceVariant,
+            ),
+          ),
+          const Spacer(),
+          _MiniToggle(
+            label: 'Простой',
+            active: mode == AppMode.simple,
+            accent: scheme.primary,
+            onTap: () => onModeChanged(AppMode.simple),
+          ),
+          const SizedBox(width: 6),
+          _MiniToggle(
+            label: 'Продвинутый',
+            active: mode == AppMode.advanced,
+            accent: scheme.secondary,
+            onTap: () => onModeChanged(AppMode.advanced),
+          ),
+          const SizedBox(width: 8),
+          _WindowButton(icon: Icons.minimize, title: 'Свернуть'),
+          _WindowButton(icon: Icons.crop_square, title: 'Развернуть'),
+          _WindowButton(icon: Icons.close, title: 'Закрыть', close: true),
+        ],
+      ),
+    );
+  }
+}
+
+class _ModeBar extends StatelessWidget {
+  final AppMode mode;
+  final ValueChanged<AppMode> onModeChanged;
+
+  const _ModeBar({
+    required this.mode,
+    required this.onModeChanged,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    final scheme = Theme.of(context).colorScheme;
+
+    return Container(
+      height: 54,
+      decoration: BoxDecoration(
+        color: scheme.surface,
+        border: Border(bottom: BorderSide(color: scheme.outline)),
+      ),
+      padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
+      child: Row(
+        children: [
+          _LargeModeButton(
+            label: 'Simple',
+            subtitle: 'one tap connect',
+            active: mode == AppMode.simple,
+            accent: scheme.primary,
+            onTap: () => onModeChanged(AppMode.simple),
+          ),
+          const SizedBox(width: 8),
+          _LargeModeButton(
+            label: 'Advanced',
+            subtitle: 'servers · split tunnel · setup',
+            active: mode == AppMode.advanced,
+            accent: scheme.secondary,
+            onTap: () => onModeChanged(AppMode.advanced),
+          ),
+          const Spacer(),
+          Container(
+            padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 6),
+            decoration: BoxDecoration(
+              color: scheme.surfaceContainerHighest,
+              borderRadius: BorderRadius.circular(999),
+              border: Border.all(color: scheme.outline),
+            ),
+            child: Text(
+              mode == AppMode.simple
+                  ? 'Quick path selected'
+                  : 'Detailed path selected',
+              style: TextStyle(
+                fontSize: 10.5,
+                color: scheme.onSurfaceVariant,
+                fontFamily: 'Consolas',
+              ),
+            ),
+          ),
+        ],
+      ),
+    );
+  }
+}
+
+class _Sidebar extends StatelessWidget {
+  final AppMode mode;
+
+  const _Sidebar({required this.mode});
+
+  @override
+  Widget build(BuildContext context) {
+    final scheme = Theme.of(context).colorScheme;
+
+    return Column(
+      crossAxisAlignment: CrossAxisAlignment.stretch,
       children: [
-        const Text(
-          'OwOCloak',
-          style: TextStyle(fontSize: 24, fontWeight: FontWeight.w700),
+        Container(
+          decoration: BoxDecoration(
+            color: scheme.surface,
+            borderRadius: BorderRadius.circular(10),
+            border: Border.all(color: scheme.outline),
+          ),
+          padding: const EdgeInsets.all(16),
+          child: Column(
+            crossAxisAlignment: CrossAxisAlignment.start,
+            children: [
+              Row(
+                children: [
+                  Container(
+                    width: 12,
+                    height: 12,
+                    decoration: BoxDecoration(
+                      color: scheme.primary,
+                      borderRadius: BorderRadius.circular(3),
+                    ),
+                  ),
+                  const SizedBox(width: 8),
+                  const Text(
+                    'Workspace',
+                    style: TextStyle(fontSize: 12, fontWeight: FontWeight.w700),
+                  ),
+                ],
+              ),
+              const SizedBox(height: 12),
+              _KeyValue(label: 'Mode', value: mode == AppMode.simple ? 'Simple' : 'Advanced'),
+              _KeyValue(label: 'Build', value: '0.9.2-dev'),
+              _KeyValue(label: 'Profile', value: 'OwONaive'),
+              _KeyValue(label: 'Latency', value: '41 ms'),
+              _KeyValue(label: 'Server', value: 'Germany / Warsaw'),
+              const SizedBox(height: 12),
+              Row(
+                children: [
+                  Expanded(
+                    child: FilledButton.tonal(
+                      onPressed: () {},
+                      child: const Text('Open settings'),
+                    ),
+                  ),
+                  const SizedBox(width: 10),
+                  Expanded(
+                    child: OutlinedButton(
+                      onPressed: () {},
+                      child: const Text('Export'),
+                    ),
+                  ),
+                ],
+              ),
+            ],
+          ),
         ),
-        const Spacer(),
-        SegmentedButton<AppMode>(
-          segments: const [
-            ButtonSegment(
-              value: AppMode.simple,
-              label: Text('Простой'),
-            ),
-            ButtonSegment(
-              value: AppMode.advanced,
-              label: Text('Продвинутый'),
-            ),
-          ],
-          selected: {mode},
-          onSelectionChanged: (values) => onModeChanged(values.first),
+        const SizedBox(height: 16),
+        const Expanded(child: AccountSection()),
+      ],
+    );
+  }
+}
+
+class _Workspace extends StatelessWidget {
+  final AppMode mode;
+  final Widget child;
+
+  const _Workspace({
+    required this.mode,
+    required this.child,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    final scheme = Theme.of(context).colorScheme;
+    final title = mode == AppMode.simple ? 'Simple mode' : 'Advanced mode';
+    final subtitle = mode == AppMode.simple
+        ? 'Fast connection path, minimal controls, and the main Connect action in the center.'
+        : 'Server selection, self-hosted setup, split tunneling, and security tools in one place.';
+
+    return Container(
+      decoration: BoxDecoration(
+        color: scheme.surface,
+        borderRadius: BorderRadius.circular(10),
+        border: Border.all(color: scheme.outline),
+      ),
+      child: Column(
+        crossAxisAlignment: CrossAxisAlignment.stretch,
+        children: [
+          Padding(
+            padding: const EdgeInsets.fromLTRB(18, 16, 18, 14),
+            child: Row(
+              crossAxisAlignment: CrossAxisAlignment.start,
+              children: [
+                Container(
+                  width: 3,
+                  height: 28,
+                  margin: const EdgeInsets.only(top: 2),
+                  color: mode == AppMode.simple ? scheme.primary : scheme.secondary,
+                ),
+                const SizedBox(width: 12),
+                Expanded(
+                  child: Column(
+                    crossAxisAlignment: CrossAxisAlignment.start,
+                    children: [
+                      Text(
+                        title,
+                        style: const TextStyle(fontSize: 18, fontWeight: FontWeight.w800),
+                      ),
+                      const SizedBox(height: 5),
+                      Text(
+                        subtitle,
+                        style: TextStyle(
+                          fontSize: 12,
+                          color: scheme.onSurfaceVariant,
+                        ),
+                      ),
+                    ],
+                  ),
+                ),
+                const SizedBox(width: 16),
+                _InfoBadge(
+                  label: mode == AppMode.simple ? 'Quick connect' : 'Manual control',
+                  accent: mode == AppMode.simple ? scheme.primary : scheme.secondary,
+                ),
+              ],
+            ),
+          ),
+          const Divider(height: 1),
+          Expanded(
+            child: ClipRRect(
+              borderRadius: BorderRadius.circular(10),
+              child: child,
+            ),
+          ),
+        ],
+      ),
+    );
+  }
+}
+
+class _KeyValue extends StatelessWidget {
+  final String label;
+  final String value;
+
+  const _KeyValue({
+    required this.label,
+    required this.value,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    final scheme = Theme.of(context).colorScheme;
+    return Padding(
+      padding: const EdgeInsets.only(bottom: 7),
+      child: Row(
+        children: [
+          SizedBox(
+            width: 72,
+            child: Text(
+              label,
+              style: TextStyle(
+                color: scheme.onSurfaceVariant,
+                fontSize: 11.5,
+              ),
+            ),
+          ),
+          Expanded(
+            child: Text(
+              value,
+              style: const TextStyle(fontFamily: 'Consolas', fontSize: 11.5),
+            ),
+          ),
+        ],
+      ),
+    );
+  }
+}
+
+class _LargeModeButton extends StatelessWidget {
+  final String label;
+  final String subtitle;
+  final bool active;
+  final Color accent;
+  final VoidCallback onTap;
+
+  const _LargeModeButton({
+    required this.label,
+    required this.subtitle,
+    required this.active,
+    required this.accent,
+    required this.onTap,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    final scheme = Theme.of(context).colorScheme;
+    return Expanded(
+      child: InkWell(
+        onTap: onTap,
+        borderRadius: BorderRadius.circular(8),
+        child: AnimatedContainer(
+          duration: const Duration(milliseconds: 150),
+          curve: Curves.easeOut,
+          padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
+          decoration: BoxDecoration(
+            color: active ? accent.withOpacity(0.15) : scheme.surfaceContainerHighest,
+            borderRadius: BorderRadius.circular(8),
+            border: Border.all(
+              color: active ? accent.withOpacity(0.28) : scheme.outline,
+            ),
+          ),
+          child: Row(
+            children: [
+              Container(
+                width: 12,
+                height: 12,
+                decoration: BoxDecoration(
+                  shape: BoxShape.circle,
+                  color: active ? accent : scheme.onSurfaceVariant.withOpacity(0.35),
+                ),
+              ),
+              const SizedBox(width: 10),
+              Column(
+                crossAxisAlignment: CrossAxisAlignment.start,
+                children: [
+                  Text(
+                    label,
+                    style: TextStyle(
+                      fontSize: 12,
+                      fontWeight: FontWeight.w700,
+                      color: active ? accent : scheme.onSurfaceVariant,
+                    ),
+                  ),
+                  const SizedBox(height: 2),
+                  Text(
+                    subtitle,
+                    style: TextStyle(
+                      fontSize: 10.5,
+                      color: scheme.onSurfaceVariant,
+                    ),
+                  ),
+                ],
+              ),
+            ],
+          ),
         ),
-      ],
-    );
-  }
-}
+      ),
+    );
+  }
+}
+
+class _InfoBadge extends StatelessWidget {
+  final String label;
+  final Color accent;
+
+  const _InfoBadge({
+    required this.label,
+    required this.accent,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    return Container(
+      padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 5),
+      decoration: BoxDecoration(
+        color: accent.withOpacity(0.12),
+        borderRadius: BorderRadius.circular(999),
+        border: Border.all(color: accent.withOpacity(0.28)),
+      ),
+      child: Text(
+        label,
+        style: TextStyle(
+          fontSize: 10.5,
+          color: accent,
+          fontWeight: FontWeight.w700,
+        ),
+      ),
+    );
+  }
+}
+
+class _MiniToggle extends StatelessWidget {
+  final String label;
+  final bool active;
+  final Color accent;
+  final VoidCallback onTap;
+
+  const _MiniToggle({
+    required this.label,
+    required this.active,
+    required this.accent,
+    required this.onTap,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    final scheme = Theme.of(context).colorScheme;
+    return InkWell(
+      onTap: onTap,
+      borderRadius: BorderRadius.circular(999),
+      child: Container(
+        padding: const EdgeInsets.symmetric(horizontal: 10, vertical: 5),
+        decoration: BoxDecoration(
+          color: active ? accent.withOpacity(0.12) : scheme.surfaceContainerHighest,
+          borderRadius: BorderRadius.circular(999),
+          border: Border.all(color: active ? accent.withOpacity(0.28) : scheme.outline),
+        ),
+        child: Text(
+          label,
+          style: TextStyle(
+            fontSize: 10.5,
+            fontWeight: FontWeight.w700,
+            color: active ? accent : scheme.onSurfaceVariant,
+          ),
+        ),
+      ),
+    );
+  }
+}
+
+class _WindowButton extends StatelessWidget {
+  final IconData icon;
+  final String title;
+  final bool close;
+
+  const _WindowButton({
+    required this.icon,
+    required this.title,
+    this.close = false,
+  });
+
+  @override
+  Widget build(BuildContext context) {
+    final scheme = Theme.of(context).colorScheme;
+    return SizedBox(
+      width: 40,
+      height: 28,
+      child: IconButton(
+        onPressed: null,
+        tooltip: title,
+        icon: Icon(icon, size: 12),
+        color: close ? scheme.error : scheme.onSurfaceVariant,
+        padding: EdgeInsets.zero,
+        visualDensity: VisualDensity.compact,
+      ),
+    );
+  }
+}
